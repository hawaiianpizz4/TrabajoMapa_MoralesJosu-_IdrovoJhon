import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app_ciclo_vida',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
